# Set the default behavior, in case people don't have core.autocrlf set.
# Git will handle the files in whatever way it thinks is best. This is a good default option.
* text=auto

# Explicitly declare text files you want to always be normalized and converted to lf line endings on checkout even on
# Windows.
*.py text eol=lf
*.txt text eol=lf
*.jinja2 text eol=lf
*.yml text eol=lf
*.ini text eol=lf
.env text eol=lf
*.md text eol=lf
*.sh text eol=lf
Dockerfile

# Declare files that will always have CRLF line endings on checkout even on Linux.
# None for now.

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
